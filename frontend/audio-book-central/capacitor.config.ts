import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Audio Book Central',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
